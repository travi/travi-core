(function (global) {
    'use strict';

    var moment = {
        unix: function (timestamp) {
            return {
                format: function (format) {}
            };
        }
    };
}(this));