{{#each updates}}
<li class="entityBlock {{=type}}">
    <ul class="actions">
        {{#each $parent.parent.data.list.actions}}
        <li class="{{=$ctx.toLower(text)}}-item">
            <form action="#" method="post">
                <label for="text">text</label><input id="text" name="someName" type="text"/>
            </form>
        </li>
        {{/each}}
    </ul>
</li>
{{/each}}