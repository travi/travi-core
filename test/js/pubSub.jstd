travi.test.testCase('PubSubTests', {
    events: travi.events,

    'test pub/sub methods exposed': function () {
        assertFunction(this.events.publish);
        assertFunction(this.events.subscribe);
        assertFunction(this.events.unsubscribe);
    }
});